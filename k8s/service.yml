apiVersion: v1
kind: Service
metadata:
  name: my-app-service
spec:
  type: LoadBalancer          
  selector:
    app: my-app      # 👈 match Deployment label
  ports:
    - port: 80
      targetPort: 3000   # 👈 match containerPort in deployment.yml
